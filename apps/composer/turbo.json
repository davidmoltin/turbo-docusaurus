{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "outputs": [
        "public/composer/**",
        ".cache/composer/**",
        ".vercel/composer/**"
      ],
      "env": [
        "NODE_ENV"
      ]
    }
  }
}
